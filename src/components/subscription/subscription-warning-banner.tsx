'use client'

// ============================================================================
// src/components/subscription/subscription-warning-banner.tsx (v9.4.3 ★★★)
// ShopAccounting — Subscription Warning Banner (DISABLED)
// ----------------------------------------------------------------------------
// ★★★ v9.4.3: این کامپوننت غیرفعال شد.
//   - قبلاً یک نوار زرد/قرمز بالای صفحه نمایش می‌داد
//   - حالا return null می‌کند (هیچ چیزی نمایش نمی‌دهد)
//   - دکمه «تمدید/ارتقا» به سایدبار (app-shell.tsx) منتقل شد
//
// ★ دلیل غیرفعال‌سازی:
//   - نوار زرد بالای صفحه مزاحم بود
//   - دکمه تمدید در سایدبار بهتر و نامحسوس‌تر است
//   - همچنین خطای ByteString (کاراکتر فارسی در هدر) را حل کردیم
//
// ★ برای فعال‌سازی مجدد (اگر خواستید):
//   - کد قدیمی را از git history برگردانید
//   - یا این فایل را با نسخه قبلی جایگزین کنید
// ============================================================================

import { useEffect } from 'react'

/**
 * ★★★ v9.4.3: این کامپونند غیرفعال است.
 *   برای حفظ سازگاری با کدهایی که آن را import کرده‌اند،
 *   این کامپوننت را نگه می‌داریم اما null برمی‌گرداند.
 */
export function SubscriptionWarningBanner() {
  // ★ هیچ کاری نمی‌کنیم — فقط null برمی‌گردانیم
  return null
}

export default SubscriptionWarningBanner
